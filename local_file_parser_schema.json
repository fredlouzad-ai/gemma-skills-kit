{
  "name": "local_file_parser",
  "description": "Parses and extracts structured data from a local JSON or CSV file string representation.",
  "parameters": {
    "type": "object",
    "properties": {
      "fileContent": {
        "type": "string",
        "description": "The raw string content of the file to parse."
      },
      "format": {
        "type": "string",
        "enum": ["json", "csv"],
        "description": "The format of the file content."
      }
    },
    "required": ["fileContent", "format"]
  }
}
